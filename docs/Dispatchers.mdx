---
id: dispatchers
title: Dispatchers
description: Dispatchers provide a way to synchronize incremental changes of policy.
keywords: [dispatcher]
authors: [dovics]
---

Dispatchers synchronize incremental policy changes using consistency algorithms like Raft, ensuring consistency across all Enforcer instances. They enable easy creation of distributed Casbin clusters.

Dispatcher functionality consists of two parts: Casbin-integrated methods called internally by Casbin, and dispatcher-specific methods for initialization, dynamic membership, and configuration changes.

:::note

Dispatchers ensure consistency of Casbin Enforcers during runtime operations. All instances must have consistent initial policy states; inconsistent initialization will cause unexpected behavior.

:::

Below is a complete list of Casbin dispatchers. Third-party contributions of new dispatchers are welcome – please notify us to have your implementation added.

```mdx-code-block
<Tabs groupId="langs">
<TabItem value="Go" label="Go" default>

import DispatcherMDTable from "@site/src/tableData/DispatcherData/DispatcherMDTable";
import {DispatcherGoData} from "@site/src/tableData/DispatcherData/DispatcherGoData";

<DispatcherMDTable data={DispatcherGoData}></DispatcherMDTable>
```

```mdx-code-block
</TabItem>
</Tabs>
```

### DistributedEnforcer

DistributedEnforcer wraps SyncedEnforcer to provide dispatcher functionality for distributed environments.

```mdx-code-block
<Tabs groupId="langs">
<TabItem value="Go" label="Go" default>
```

```go
    e, _ := casbin.NewDistributedEnforcer("examples/basic_model.conf", "examples/basic_policy.csv")
```

```mdx-code-block
</TabItem>
</Tabs>
```
